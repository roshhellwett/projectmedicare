// Server-side Supabase clients, shared with the Railway apps.
export {
  createAdminClient,
  createPublicClient,
  isSupabaseConfigured,
  CAMP_BUCKET,
  GALLERY_BUCKET,
} from "@jm/shared/supabase";
