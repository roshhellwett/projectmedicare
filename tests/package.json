{
  "name": "@jm/tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Smoke and end-to-end tests for the whole project.",
  "scripts": {
    "test:smoke": "node smoke/run.mjs",
    "test:e2e": "playwright test",
    "test:e2e:install": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
